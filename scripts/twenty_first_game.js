let game_display = document.querySelector(".game_display")
let animated_button = document.querySelector(".animated_button")
let row = document.querySelectorAll(".row")
row.forEach(element => {
    animated_button.onclick = ()=> {
        // let rand1 = Math.floor(Math.random()*)
    }
});